mixin pricing-card(cardPrice, cardCount, cardTitle, cardText)
    .col-lg-3.col-md-6
        .card-top
            span $#{cardPrice}
            p #{cardCount}  Class
        .card-main
            h4 #{cardTitle}
            p #{cardText}
        button.btn-primary.btn
            span sign up now